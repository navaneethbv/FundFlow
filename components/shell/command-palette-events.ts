/** Shared event name so the shell search button can open the
 * palette without lifting its open/closed state out of the component. */
export const OPEN_COMMAND_PALETTE_EVENT = "fundflow:open-command-palette";
